name: flutter_corner_shape_example
description: Example app for the flutter_corner_shape package.
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_corner_shape:
    path: ../
  # Added only for the side-by-side comparison page.
  figma_squircle: ^0.6.3

flutter:
  uses-material-design: true
